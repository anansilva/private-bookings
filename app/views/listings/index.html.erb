<%= render(HeadingComponent.new.with_content("Casas")) %>

<section>
    <div class="mt-8 grid grid-cols-2 lg:grid-cols-4 gap-x-4 gap-y-8">
      <%= render(ListingComponent.with_collection(@listings)) %>
    </div>
</section>
